syntax = "proto3";

package nlp;

service Nlp {
    rpc GiveTag (FilmDescription) returns (FilmTags);
}

message FilmDescription {
    string description = 1;
}

message FilmTags {
    string tags = 1;
}